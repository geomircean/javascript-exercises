export function argumentsOfAFunction() {
  /**TODO: return the number of arguments passed to a function */
}
export function palindrome(param) {
  /** TODO: Return true or false if param is a palindrome */
}

export function leapYear(year) {
  /** Calculate if a given year is a leap year or not */
}

export function callMeBack(a, b, fn) {
  /**TODO: callMeBack receives as an argument two numbers function. Sum a and b
   * and pass the result to fn.
   */
}

export function fibbonaci(n) {
  /** TODO: compute the first n numbers of the fibbonaci sequence */
  return [];
}

// console.log(fibbonaci(5));
