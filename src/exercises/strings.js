export function concatenation(strA, strB) {
  /** Write a program that returns a concatenation of 2 strings given as arguments */
}

export function reverseString(str) {
  /** Write a program that takes an input and returns another string with
   * the letters reversed
   */
}

export function countCharacters(str) {
  /** Write a funciton that counts the number of alphabets, digits
   * and special characters in a string
   */
  return {
    alphabets: 0,
    digits: 0,
    special: 0
  };
}

export function vowelsAndConsonants(str) {
  /** Write a function that counts the number of vowels and consonants */
  return {
    vowels: 0,
    consonants: 0
  };
}

export function replaceChar(inputStr, charToReplace, replChar) {
  /** Given the an input string inputStr, find all occurences of a given character charToReplace
   * replace them with replChar and return new string;
   */
  let outputStr = "";
  return outputStr;
}
